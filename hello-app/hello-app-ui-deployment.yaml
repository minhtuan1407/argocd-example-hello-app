apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-app-ui
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: hello-app-ui
  template:
    metadata:
      labels:
        app: hello-app-ui
    spec:
      containers:
      - image: gcr.io/google-samples/hello-app:2.0
        name: hello-app-ui
        ports:
        - containerPort: 8080
